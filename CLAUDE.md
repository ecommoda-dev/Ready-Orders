<div dir="rtl" style="text-align: right;">

# طابور الأوردرات الجاهزة — Ready Orders (`Ready-Orders`)

![version](https://img.shields.io/badge/worker-v1.4.0-blue)

**بتعمل إيه:** بترجّع الأوردرات اللي حالتها **`Ready`** — الشحنة الأصلية
(`custom.manual_status`) أو دورة الاستبدال/الاسترجاع (`custom.status_2_r_e`).
**وبترجّع كمان حالة أوردر معيّن بالـ ID أو بالاسم** (`lookup_orders`) — لتاب
«جرد المكتب» في الواجهة.
**مين بيستخدمها:** الشحن والتحصيل
**الإصدار:** `1.4.0` (`WORKER_VERSION` في `index.js`)

> 🔴 **الريبو ده Worker وبس — مفيش واجهة هنا ومفيش نسخة مستقلة** (قرار أحمد
> 15-09-2026، بنفس سابقة `Package-Transfer-To-Office`). الواجهة الوحيدة هي
> **`ready-orders.html`** جوّه `Delivery-COD-Operations-Center`.
> ⛔ ممنوع يتضاف `index.html` هنا: نسخة مستقلة معناها مفتاح `localStorage` تاني
> وسر تاني وشاشة دخول تانية — وكلهم بنود مفتوحة في الأدوات اللي اتحوّلت للهبات،
> ومفيش سبب نفتحهم من جديد.

> ⚠️ **و`lookup_orders` (من `1.2.0`) مابيكسرش ده** — قراءة بحتة كمان،
> ومستهلكها تاب «جرد المكتب» في الواجهة. تفاصيلها تحت.

> 🔴 **قراءة بحتة — صفر كتابة.** مفيش `metafieldsSet` ومفيش `orderCancel`
> ومفيش صف D1، **ومفيش `[[d1_databases]]` في `wrangler.toml` أصلاً**.
> ⛔ أول ما يتضاف أي فعل: الـ binding يترجع (اسمه `DB` **بالحرف** — أي اسم تاني
> = كتابة فاشلة بصمت)، و`tool`/`type` **يتسجّلوا في `ecommoda-constants` §7
> قبل أول `writeLog`** (Rule 7).

## الروابط

```
الـ Worker : https://ready-orders-worker.ecommoda-dev.workers.dev
الواجهة    : https://ecommoda-dev.github.io/Delivery-COD-Operations-Center/ready-orders.html
             («قسم الجاهز للشحن» — اتسمّت كده في هب الشحن v1.1.0)
D1         : ❌ الأداة مابتلمسش السجل
مجموعة السر : delivery_cod_ops   ← 🔴 مش سر فريد للأداة دي · ومستقلة عن المخزن
```

## Endpoints

| `action` | Method | بيعمل إيه |
|---|---|---|
| `get_config` | GET | `WORKER_VERSION` — الواجهة بتقارنه بـ`min` |
| `diag` | GET | فحص ذاتي بلا كتابة — **مصفوفة `[{ ok, label, detail, hint }]`** |
| **`get_ready_queue`** | GET | أوردرات `Ready` (الماكينتين) بحقولها الخام — **من `01/04/2026` وطالع** (من `1.4.0`) |
| **`lookup_orders`** | **POST** | حالة أوردرات معيّنة بالـ ID أو بالاسم — **من `1.2.0`** |

> ⚠️ **و`get_ready_queue` بيرجّع `note` من `1.3.0`** — مصدر عمود «ملحوظات»
> في الواجهة. `lookup_orders` **مابيرجّعهوش**: مستهلكه تاب الجرد وبيسأل عن
> الحالة بس، وحقل بلا مستهلك = سطح بلا لازمة.

> 🔴 **انحراف مقصود ومكتوب:** مفيش `check_employee`/`register_pin`/
> `verify_employee`/`log_logout`/`get_employees`/`get_logs*`. الأداة مابتكتبش
> في D1 فمفيش سجل يتفلتر، والدخول بيحصل **مرة واحدة** في `index.html` بتاع
> الهب عبر Worker التغليف. نقطة دخول تانية هنا = سطح هجوم بلا مستهلك (نفس قرار
> `order-sku-barcode-printer-worker` و`package-transfer-to-office-worker`).

## 🔴 الاستعلام بيسأل عن الحالة + أرضية التاريخ

```
created_at:>='<بداية 01/04/2026 بتوقيت القاهرة>'   ← الأرضية (من 1.4.0)
  AND metafields.custom.manual_status:'Ready'      ← الشحنة الأصلية
  AND … أو … status_2_r_e:'Ready'                  ← دورة الاستبدال/الاسترجاع
```

### 🔴 أرضية التاريخ — `01/04/2026` (من `1.4.0` · طلب أحمد 17-09-2026)

`QUEUE_MIN_CREATED_DAY = '2026-04-01'`، والاستعلام بيتركّب في مكان واحد
(`queueQ`) للماكينتين.

- 🔴 **ودي أول فلترة زيادة في الأداة دي** — وقاعدة «الحالة وبس» تحت اتكتبت
  ضد فلتر **تشغيلي** (زون · مندوب · تغليف) يخلّي أوردر شغّال **يختفي في
  صمت**. الأرضية نوع تاني: قرار أحمد إن اللي قبل التاريخ ده **مش شغل
  المحطة** أصلاً (أوردرات دورة قديمة عالقة كانت بتملا الطابور بمئات
  الصفوف بادجها «متأخر ١٧١ يوم»).
- 🔴 **والاستبعاد مُعلَن في تلات أماكن**: في **رد الـ endpoint** نفسه
  (`minCreatedDay` · `minCreatedAt`) · في `?action=diag` باللحظة المحسوبة ·
  وفي الواجهة **جنب الرقم الكبير** (شيب «الطابور من 01/04/2026» بيتقرا من
  الرد مش مكتوب في الصفحة).
- 🔴 **والتاريخ يوم بتوقيت القاهرة — بيتحوّل للحظة UTC بـ`Intl`**
  (`cairoDayStartUtcISO` → `2026-03-31T22:00:00Z`).
  ⛔ **وممنوع نبعت `created_at:>=2026-04-01` مجرّد**: تفسير التاريخ المجرّد
  عند شوبيفاي مش مضمون إنه بتوقيت المتجر، ولو اتفسّر UTC فأوردرات يوم
  `01/04` من ١٢ بالليل لحد ٢-٣ الفجر بتوقيت القاهرة **بتقع بره الأرضية**
  وبتختفي — يوم ناقص بضع ساعات بلا أي رسالة.
  ⚠️ والدالة بتلفّ دورتين عن قصد — التانية بتصحّح الإزاحة لو اللحظة
  الأولى وقعت على الجهة التانية من تحويل التوقيت الصيفي.
- ⛔ **وأي تعديل في القيمة ينزل على `Shipped-Orders` في نفس التمريرة** —
  أرضيتان مختلفتان في طابورين على نفس الشاشة بتخلّي الرقمين بيتقروا
  بقاعدتين.
- 🔴 **و`ready.min` اترفع لـ`1.4.0` في نفس التسليم** — على Worker أقدم
  الطابور بيرجع **بكامل الأوردرات القديمة**، يعني القرار **مش مطبّق
  أصلاً**، والحاجة الوحيدة اللي بتقول كده على الشاشة هي **غياب** شيب
  الأرضية — وغياب حاجة مابيتلاحظش. فالحارس بيسمّي الأداة والنسخة.

الاتنين بالتوازي، والنتيجة بتتدمج **بالـ id** (فمفيش تكرار لو الأوردر في
الاتنين). **والماكينة والتنبيهات والتقسيمة والمبلغ المستحق بيتحسبوا في الواجهة**
— في `shared/shell.js` §QUEUE-RULES.

- 🔴 **ليه الاشتقاق مش هنا:** الشاشة الرئيسية بتعرض **رقم** والصفحة بتعرض
  **قايمة**، والاتنين لازم يعدّوا بنفس القاعدة بالحرف. نسخة في الـ Worker ونسخة
  في الواجهة = الرقمين بيفترقوا **في صمت** (درس R1 · v1.11.0 في هب المخزن:
  الرئيسية قالت ٦٦ والصفحة فتحت على ٦).
- 🔴 **ومفيش فلترة زيادة في الاستعلام بقرار.** الأداة **عرض بحت** ومالهاش بوابة
  كتابة تحمي حد من حاجة، وأي شرط زيادة معناه أوردر **بيختفي في صمت** والموظف
  بيسأل «فين؟» ومفيش إجابة. القاعدة: **يبان وعليه علامة** (قاعدة ١٣ و١٤ في
  `ecommoda-order-lifecycle`).
- ⛔ **وممنوع فلتر على ميتافيلد مش قابل للفلترة.** `package_whereabouts_s1`
  مثلاً بيتجاهَل **في صمت** ويرجّع المتجر كله (١٠٬٠٠٠ صف — مقيس 15-09-2026).
  الحقلين فوق **مؤكَّدين قابلين للفلترة**.
- ⚠️ **والتنصيص مهم** — `Cairo+Giza` فيها `+` وليها معنى في صيغة البحث، والعادة
  دي متبعة في كل الفلاتر هنا.

## الحقول الراجعة — عقد مع الواجهة

`shapeOrder` بيرجّع: `orderId` (رقمي) · `orderGid` · `orderName` · `createdAt` ·
`cancelledAt` · `fulfillment` · `financial` · `customer` · **`note`** ·
**`address1`** · **`address2`** · `city` · `province` · `itemsQty` · `total` · `currency` ·
`zone` · `courier` · `s1` · `s2` · `packedAtS1`/`S2` · `packedByS1`/`S2` ·
`whereaboutsS1`/`S2` · `trackingLegacy`.

### 🔴 `address1`/`address2` — جداد في `1.1.0` (طلب أحمد 16-09-2026)

الواجهة بقى فيها **عمود «العنوان»** بعد عمود العميل، والحقلين دول مصدره.

- 🔴 **بيرجعوا خام ومنفصلين — ⛔ ممنوع نركّبهم هنا في نص واحد.** الواجهة
  بتعرضهم في خلية واحدة، لكنها **محتاجة تعرف إيه اللي ناقص** عشان تقول
  **«بلا عنوان»** صراحةً بدل ما تعرض فاصلة معلّقة على سطر فاضي. التركيب في
  الـ Worker بيشيل المعلومة دي **قبل** ما توصل للشاشة.
- ✅ **وصفر زيادة في تكلفة الاستعلام.** التكلفة عند شوبيفاي بتتحسب
  **بالكائنات والـ connections**، مش بعدد الحقول البسيطة — و`shippingAddress`
  كان **متحدّد أصلاً**. فـ`QUEUE_PAGE_SIZE` فضل **٤٠** زي ما هو، ومحتاجش
  إعادة قياس.
- ⛔ **وممنوع أي backtick جوّه `QUEUE_QUERY`** — دي template literal، وأول
  backtick بيقفلها والملف **مابيعديش الـ parse أصلاً**. (حصل فعلاً وقت كتابة
  التعليق على السطر ده.)
- 🔴 **و`ready.min` بقى `1.2.0` من تسليم الجرد** — الشرح في قسم
  `lookup_orders` تحت. التدهور على Worker أقدم **محصور في سبب** قسم
  «موجودة خطأ» بس، والجرد نفسه شغّال بالكامل.

- 🔴 **و`ready.min` في `shared/shell.js` اترفع لـ`1.1.0` في تسليم `1.1.0`** —
  على Worker `1.0.0` عمود العنوان بيقول **«بلا عنوان» على كل صف**، وعمود
  كامل بجملة تحذير بيتقري **عطل في الأداة** مش «Worker قديم». التدهور نفسه
  **معلَن ومحصور** (باقي الأعمدة والطابور شغّالين بالكامل)، والحارس بيسمّي
  السبب بدل ما الموظف يدوّر.

- ⚠️ **الشكل ده عقد**: `dcoShapeRow` و`dcoFlags` و`dcoCod` في الـ shell بيقروا
  المفاتيح دي **بالاسم**. تغيير اسم مفتاح هنا = عمود بيرجع `—` على كل صف
  **بلا أي خطأ في الكونسول**.

### 🔴 `note` — جديد في `1.3.0` (طلب أحمد 17-09-2026)

الواجهة بقى فيها **عمود «ملحوظات»** بعد عمود العنوان، ومصدره حقل **Notes**
على الأوردر في شوبيفاي (`Order.note`).

- 🔴 **`o.note || null` مش `o.note`** — النص الفاضي (`''`) لازم يتقري «مفيش
  ملحوظة» بالظبط زي الغياب. من غير الحارس ده الواجهة بتفرّق بين حالتين
  **مالهمش أي فرق تشغيلي**، والخلية بتبان «فيها حاجة» وهي فاضية.
- ✅ **وصفر زيادة في تكلفة الاستعلام** — `note` **سكالر على الأوردر نفسه**،
  والتكلفة عند شوبيفاي بتتحسب بالكائنات والـ connections. `QUEUE_PAGE_SIZE`
  فضل **٤٠** ومحتاجش إعادة قياس (نفس منطق `address1`/`address2` فوق).
- 🔴 **وبيرجع خام** — بلا قص وبلا تنظيف سطور. التقصير البصري في الواجهة،
  والـ Worker مالوش رأي في شكل النص.
- ⛔ **وممنوع أي backtick في تعليقه** — التعليق جوّه `ORDER_FIELDS` وهي
  template literal (نفس الفخ اللي فوق، وحصل تاني وقت كتابة السطر ده).
- 🔴 **و`ready.min` في `shared/shell.js` اترفع لـ`1.3.0` في نفس التسليم** —
  على Worker أقدم العمود بيقول `—` على **كل** صف، وده بيتقري «الأداة
  بايظة» مش «الحقل مش موجود». والحارس بيسمّي الأداة والنسخة المطلوبة.
  ⚠️ **والتدهور محصور ومُعلَن**: باقي الأعمدة والطابور والجرد شغّالين
  بالكامل — اللي بيقع هو **خانة الملحوظات** لوحدها.
- ⛔ **ومفيش فلتر على `note` في الاستعلام** — نفس قاعدة «الفلترة في
  الواجهة» (قرار ③): أي شرط زيادة هنا معناه أوردر بيختفي في صمت.
- 🔴 **`orderId` رقمي إلزامي** (`worker-builder` Step 5) — الواجهة بتبني بيه
  رابط الأدمن، والـ`gid` مايبنيش رابط.
- 🔴 **`currentTotalPriceSet` مش `totalPriceSet`** (`order-lifecycle` قاعدة ٣) —
  التاني بيمسك القيمة الأصلية وبيفضل زي ما هو بعد أي تعديل أو مرتجع، يعني رقم
  أكبر من اللي المندوب بيحصّله.
- ⚠️ **`currentSubtotalLineItemsQuantity`** بيجمع `currentQuantity` مش
  `quantity` — فأوردر الاستبدال بيرجّع **البديل بس** مش القطعة الراجعة معاه.

## 🔴 `lookup_orders` — «الكود ده لأنهي أوردر وحالته إيه؟» (من `1.2.0`)

```
POST /?action=lookup_orders
{ "ids": ["6959895839042", …], "names": ["#55001", …] }
→ { ok, results: [{ key, kind:'id'|'name', found, order|null }], truncated }
```

**المستهلك واحد ومعروف: تاب «جرد المكتب»** في `ready-orders.html`. الجرد
بيطلّع تلات أقسام، والتالت («موجودة خطأ») هو الطرد اللي اتعمله سكان وهو
**مش في قسم الجاهز للشحن** — والقسم ده بلا حالة حقيقية بيبقى بند بيقول «فيه
حاجة» وبس، وتكلفته **فحص يدوي لكل طرد** على شوبيفاي (قاعدة ١٤).

- ⛔ **ومن غير المستهلك ده كان ممنوع تتضاف** — نقطة endpoint بلا مستهلك =
  سطح بلا لازمة (نفس قرار `get_logs*` فوق).
- 🔴 **قراءة بحتة** زي باقي الـ Worker: صفر ميوتيشن وصفر صف D1.

### 🔴 الـ ID بـ`nodes(ids:)` — مش ببحث

الباركود على الطرد فيه **Order ID الطويل** (قرار أحمد 16-09-2026)، و
`nodes(ids:)` بتجيب **٢٥ أوردر في استعلام واحد** بمطابقة **مباشرة** على
المفتاح.

- ⛔ **و`orders(query: "id:…")` كان غلط لتلات أسباب**: استعلام لكل كود ·
  أغلى · وهو **بحث** بيرجّع «أقرب نتيجة» (`shopify-graphql-helper`
  § name-search)، فطرد غلط كان ممكن يتقري صح.
- 🔴 **والاسم لسه بحث** (مالوش مدخل مباشر) — فالرد بيتفلتر بمطابقة
  **حرفية** على `name` بعد الرجوع. من غير الفلتر ده، بحث `#5500` بيرجّع
  `#55001` والشاشة بتقول عنه إنه هو.
- 🔴 **والرد فيه مدخل لكل كود اتبعت — حتى اللي مالقيناهوش** (`found:false`).
  حذف المفقود كان بيخلّي الواجهة تربط بالترتيب، وترتيب ناقص عنصر بيزحلق
  **كل** الباقي: حالة أوردر بتتعرض على أوردر تاني. **الربط بالمفتاح.**
- ⚠️ والتطبيع والتفريد **قبل** أي نداء — نفس الكود ممكن يتعمله سكان مرتين.
- ⚠️ والـ `id` أقل من ٦ أرقام أو أكتر من ٢٠ **بيترفض قبل النداء** — مفيش
  Order ID بالشكل ده، والاستعلام بيه مضمون إنه بلا نتيجة.

### السقوف

```
LOOKUP_MAX_ITEMS   = 200   ← سقف العناصر في النداء الواحد (الزيادة بترجع truncated)
LOOKUP_NODES_CHUNK = 25    ← ids لكل استعلام
LOOKUP_NAME_BATCH  = 5     ← بحوث الاسم بالتوازي
```

- ⚠️ **الاقتطاع مُعلَن** (`truncated: true`) والواجهة بتقوله — قايمة مقصوصة
  في صمت بتخلّي الموظف يفتكر إن الباقي «مالقيناهوش».
- ⚠️ **وحقول الاستعلام أقل من الطابور عن قصد** — مفيش عنوان ولا مبلغ ولا
  أوقات تغليف: القسم ده بيجاوب على سؤال واحد («ده إيه وحالته إيه»)، وحقل
  بلا مستهلك في الشاشة = تكلفة استعلام بلا مقابل.

### 🔴 `POST` في CORS

`Access-Control-Allow-Methods` بقى `GET, POST, OPTIONS`. من غير `POST`
الـ preflight بيرفض النداء **والواجهة بتقول «تعذّر الوصول» على Worker
شغّال** — أسوأ رسالة ممكنة.
⚠️ ونفس الأكشن على `GET` بيرجّع **`405` باسمه** مش `404` «أكشن مش معروف»:
التانية بتخلّي الواجهة تفتكر إن الـ Worker نسخة قديمة، والحقيقة إن الطريقة غلط.

## 🔴 سلسلة السقوف

```
① الواجهة    CHUNK           = —    ← مفيش دفعة: الأداة عرض بحت
② الـ Worker  MAX_BATCH       = —    ← مفيش endpoint بياخد مصفوفة
③ شوبيفاي     QUEUE_PAGE_SIZE = 40   ← تكلفة، مش عدد
              QUEUE_MAX_PAGES = 12   ← حارس حلقة (٤٨٠ أوردر لكل ماكينة)
```

- 🔴 **٤٠ مش ٥٠:** القياس الحيّ الوحيد لشكل الاستعلام ده هو
  `package-transfer-to-office` (٥٠ نود × **١٠** ميتافيلد، 15-09-2026، بلا
  `MAX_COST_EXCEEDED`). الاستعلام هنا بـ**١١** ميتافيلد + حقلين سكالر زيادة،
  والاستعلام اللي تكلفته أعلى من السقف **بيترفض بالكامل** ويرجّع دفعة فاضية
  برسالة مبهمة — مش بيتقص. فالنزول خطوة **آمن بالتعريف**، والطلوع محتاج قياس.
- ⛔ **متطلّعهاش من غير ما تقرا `actualQueryCost` فعليًا** — و`?action=diag`
  بيعرض `throttleStatus` بتاع آخر استعلام عشان الاقتراب من السقف يبان **قبل**
  ما ينفجر.
- ⚠️ **الاقتطاع بيتبلّغ** (`truncated: true`) والواجهة بتطلّع بانر أصفر
  **منفصل** عن بانر الفشل.

## CORS

**Option B — قايمة صارمة.**

```js
const ALLOWED_ORIGINS = ['https://ecommoda-dev.github.io'];
```

> ⚠️ **انحراف موثّق عن `ecommoda-worker-builder` Step 3** (اللي بيقول أداة
> قراءة-فقط → wildcard). الأداة قراءة بحتة فعلاً، **ومع ذلك على Option B** —
> والسبب مش الكتابة: الرد بيحمل **اسم العميل ومحافظته ومبلغ التحصيل** لكل
> أوردر، ومستهلكه **واحد معروف**. القايمة الصارمة تكلفتها صفر ومكسبها حقيقي.
> ⚠️ والحماية الفعلية لسه `WORKER_SECRET` — الـ CORS بيحمي متصفح مش سيرفر.

## التوقيت

🔴 **`Africa/Cairo` يتحسب بـ`Intl`، مايتكتبش ثابت** (`ecommoda-constants` §13).
مصر بترجع UTC+2 يوم **29-10-2026**، وأي إزاحة مكتوبة ثابت بتبقى غلط **من غير
ما الأداة تشتكي**. الأداة دي **مش في نطاق البند ده** من أول يوم.

## النشر — ✅ **اتعمل (اتأكد بالعين 16-09-2026)**

> ✅ **الـ Worker منشور وشغّال:** اتعمل بالاسم الصح · Workers Builds مربوط على
> `main` · **`Build watch paths` = `wrangler.toml` + `index.js`، كل واحد في
> chip لوحده** · `WORKER_SECRET` و`CLIENT_ID`/`CLIENT_SECRET` متحطّين ·
> و**الـ deployment النشط = دمج PR #1، يعني `1.1.0`**.
> ✅ **والدليل العملي:** `ready-orders.html` اتفتحت حيًا وطلّعت **١٢٦ أوردر
> بعناوين حقيقية** — يعني السر وOAuth والاستعلام كلهم شغّالين من الأول للآخر.
> ⚠️ **الخطوة ⑤ لسه مفتوحة** — تسجيل الأداة في مجموعة `delivery_cod_ops` في
> `ecommoda-constants`. عضو غير مسجّل = إجراء التدوير بيتكسر **بصمت**.

> 🔴 **و`1.2.0` لسه محتاج نشر + Promote (16-09-2026).** الـ deployment
> النشط `1.1.0`، و`ready.min` في الهب بقى `1.2.0` في نفس التسليم — فقبل
> الـ Promote الصفحة بتطلّع تحذير «Worker نسخة قديمة» **باسم الأداة**،
> وزرار «استعلام عن الحالة الفعلية» في الجرد بيقول إن النسخة أقدم من
> `1.2.0`. ⚠️ والتدهور **محصور**: الطابور وتاب الجرد (سكان · مضبوطة ·
> مفقودة · تصدير) شغّالين بالكامل.
> ⚠️ **والـ watch paths مش محتاجة تعديل** — التمريرة دي لمست `index.js` بس.

### الخطوات (للمرجع — وللـ Workers اللي لسه ما اتعملتش)

| # | الخطوة | من غيرها |
|---|---|---|
| ① | إنشاء Worker باسم **`ready-orders-worker`** بالظبط | الرابط في `DCO_WORKERS.ready.url` مايردّش |
| ② | ربط Workers Builds على الريبو ده (`main`) | أي push مابينشرش |
| ③ | **`WORKER_SECRET` = قيمة مجموعة `delivery_cod_ops`** → **Promote** | كل نداء `401` |
| ④ | `CLIENT_ID` + `CLIENT_SECRET` → **Promote** | OAuth بيفشل والطابور بيقع |
| ⑤ | تسجيل الأداة في **مجموعة `delivery_cod_ops`** في `ecommoda-constants` → `references/secret-groups.md` | إجراء التدوير بيتكسر **بصمت** |

> 🔴 **③ أهم واحدة، والفخّ إن `WORKER_SECRET` هنا مش قيمة فريدة.** الهب بيبعت
> **سر واحد** لتلات Workers من مفتاح `localStorage` واحد (Standards #39):
> Worker الدخول + الطابورين. سر خاص بالأداة = `401` من الأداة دي بس —
> والشاشة بتفتح عادي، والدخول شغّال، **وطابورها لوحده هو اللي بيقع**.
> ⚠️ **والمجموعة مستقلة عن `warehouse_ops`** (قرار أحمد 15-09-2026) — محطة
> الشحن أجهزتها غير أجهزة المخزن، فتسريب من جهاز بيمسّ محطة واحدة بس.
> ⚠️ **وثمنه مُعلَن:** الجهاز اللي عليه محطة المخزن **مش** مضبوط هنا
> تلقائيًا — السر الجديد بيتلزق مرة واحدة على كل جهاز.
> ✅ **والأداة دي مالهاش نسخة مستقلة** — فمفيش سر قديم لازم يتلزق في مكانين.
> ⚠️ `?action=diag` بيثبّتها: **بصمة السر القصيرة** لازم تطابق باقي أعضاء
> المجموعة (سرّين مختلفين بنفس الطول شكلهم واحد، فالطول لوحده مش كافي).

> ⚠️ **`[vars]` في `wrangler.toml` بس** — ممنوع تضيف `SHOP_DOMAIN` بإيدك في
> الداشبورد لأداة منشورة من git: أول push بيدهس القيمة اليدوية (واقعة موثّقة
> على `stylebox-price-sync-worker` · 12-09-2026).

## فحص المتصفح

الفحص بتاع الأداة دي في ريبو الهب (الواجهة عايشة هناك):

```bash
cd Delivery-COD-Operations-Center
node docs/rules-check.mjs       # ٩٠ بند منطق (§QUEUE-RULES + §AUDIT-RULES) — بلا تنصيب
npm i playwright postcss --no-save
node docs/queues-check.mjs      # ٢٣٣ بند للهب كله — §⑨ منها لتاب الجرد
```

> ⚠️ **الفحصان بيمسكوا حاجتين مختلفتين:** `queues-check` بيشغّل الصفحة على
> بيانات كاملة (بيمسك الربط بين الرقم والقايمة)، و`rules-check` بيدّي للدوال
> مدخلات **باظة وحدّية** (`null` · تاريخ غلط · تحوّل التوقيت الشتوي) — وهي
> الحالات اللي بتنتج «رقم غلط شكله سليم».

## مسائل مفتوحة

- ✅ **خطوات النشر اتعملت** (16-09-2026) — الـ Worker منشور على `1.1.0`
  والواجهة شغّالة حيًا عليه.
- 🔴 **تسجيل الأداة في مجموعة `delivery_cod_ops`** في `ecommoda-constants` →
  `references/secret-groups.md` — **لسه مفتوح**. عضو غير مسجّل = إجراء التدوير
  بيتكسر **بصمت**: بتغيّر اتنين وتنسى التالت، والنتيجة `401` في أداة محدش
  هيعرف سببها.
- 🔴 **نشر `1.4.0` والـ Promote — مفتوح.** فيه **تلات تسليمات** ما اتنشروش:
  `1.2.0` (`lookup_orders` — تاب الجرد) · `1.3.0` (`note` — عمود
  «ملحوظات») · و**`1.4.0` (أرضية `01/04/2026`)**. الـ deployment النشط لسه
  `1.1.0`، و`ready.min` في الهب بقى `1.4.0` — فالصفحة بتقول «Worker نسخة
  قديمة» **باسم الأداة** لحد النشر.
  ⛔ **وقبل النشر الأرضية مش مطبّقة خالص** — الطابور بيرجع بالأوردرات
  القديمة زي ما هو، وشيب «الطابور من …» مابيظهرش أصلاً.
- 🟡 **`lookup_orders` بلا سقف زمني لكل كود** — السقف على العدد (٢٠٠) وعلى
  الدفعة (٢٥ id / ٥ اسم في المرة). جرد بيطلّع ٢٠٠ كود غلط حالة **مش
  متوقعة** (معناها إن المكتب فيه ٢٠٠ طرد مش المفروض يكون فيه)، وقياس
  التكلفة الحقيقي لسه ما اتعملش. ⛔ ومتطلّعش السقف من غير قياس
  `actualQueryCost` زي `QUEUE_PAGE_SIZE` بالظبط.

- 🟡 **`read_all_orders` مش من صلاحيات التطبيق** — شوبيفاي بتخفي أي أوردر أقدم
  من **٦٠ يوم** عن تطبيق مالوش الصلاحية، **بلا أي خطأ**. طابور «جاهز» بيتفضّى
  أول بأول فالأثر محدود، **بس الأوردر القديم العالق في `Ready` هو بالظبط اللي
  الطابور موجود عشان يمسكه**. بند في `?action=diag` بيقولها بالاسم. القرار لأحمد.
- 🟡 **مفيش «منذ إمتى وهو `Ready`»** — مفيش ميتافيلد لوقت تحوّل الحالة، والقاعدة
  ٩ في `ecommoda-order-lifecycle` بتقول إن المدد بتتاخد من **سجل D1**. العمود
  المعروض دلوقتي «الوقت منذ التغليف» وهو اسمه بيقول اللي بيقيسه بالظبط.
- 🟢 **الأداة مابتكتبش أي صف في D1** — فمفيش أي قيمة `tool`/`type` مطلوب
  تسجيلها لها في `ecommoda-constants` §7. (صف الدخول بتاع الهب حاجة تانية،
  وبيتسجّل من Worker التغليف.)

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | **v3.3.0** |
| ecommoda-constants | **v2.6.0** |
| ecommoda-order-lifecycle | **v1.8.0** |
| shopify-graphql-helper | **v1.1.0** |
| ecommoda-html-builder | **v6.6.0** |

آخر مطابقة: 17-09-2026 · الـ Worker `1.4.0`
✅ **اتقفل 16-09-2026:** إنشاء الـ Worker + ربط Builds + `Build watch paths`
+ `WORKER_SECRET` + `CLIENT_ID`/`CLIENT_SECRET` + **نشر `1.1.0`**.
🔴 معلّقة: **نشر `1.4.0` + Promote** (وجوّاه `1.2.0` و`1.3.0` اللي لسه ما اتنشروش) ·
**تسجيل مجموعة `delivery_cod_ops` في `ecommoda-constants`**

---

آخر تحديث: 17-09-2026 — `1.4.0` (**أرضية تاريخ الطابور** — طلب أحمد:
الاستعلام بقى فيه `created_at:>=` جنب شرط الحالة، والطابور بيبدأ من
أوردرات **`01/04/2026`** واللي أقدم **مابيتجابش**. اليوم بتوقيت القاهرة
بيتحوّل للحظة UTC بـ`Intl` (`cairoDayStartUtcISO`) مش بيتبعت مجرّد ·
والأرضية بترجع في الرد (`minCreatedDay`/`minCreatedAt`) وفي `diag` عشان
الاستبعاد مايبقاش صامت · و`ready.min` بقى `1.4.0`)
⚠️ **ومحتاج نشر وPromote** — الـ deployment النشط `1.1.0`.

`1.3.0` (**`note` في `get_ready_queue`** — حقل
Notes بتاع الأوردر على شوبيفاي، مصدر عمود «ملحوظات» في الواجهة ·
صفر زيادة في تكلفة الاستعلام · و`ready.min` بقى `1.3.0` في نفس التسليم)
⚠️ **ولسه محتاج نشر وPromote** — الـ deployment النشط `1.1.0`.

`1.2.0` (**`lookup_orders` (POST)** — حالة أوردرات
معيّنة بالـ ID (بـ`nodes(ids:)`) أو بالاسم (ببحث + مطابقة حرفية)، لتاب «جرد
المكتب» في الواجهة · و`POST` دخلت في CORS · و`ready.min` بقى `1.2.0`)
⚠️ **ولسه محتاج نشر وPromote** — الـ deployment النشط `1.1.0`.

`1.1.0` (`address1`/`address2` في `shapeOrder` —
عمود «العنوان» في الواجهة · `ready.min` اترفع لـ`1.1.0` في نفس التسليم)
✅ **ونُشر فعليًا في نفس اليوم** — الـ deployment النشط على Cloudflare هو دمج
PR #1، والواجهة بتعرض العناوين حيًا.

</div>
